# The example adapter doesn't really require a test,
# but we leave a placeholder test file since otherwise
# the project won't compile. You should only do this if
# you are confident the resource doesn't need any tests.
